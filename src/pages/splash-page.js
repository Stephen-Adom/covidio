import styles from '../styles/Splash.module.css';

const SplashScreen = () => (
  <section className={styles['splash-container']}>
    <h1 className={styles.logo}>Covidio</h1>
  </section>
);
export default SplashScreen;
